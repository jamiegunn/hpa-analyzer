# TODO: switch to headless service someday
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-payments
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    app: payments
