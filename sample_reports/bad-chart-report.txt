====================================================================================================
                           HELM CHART / KUBERNETES / JVM QUALITY ANALYSIS                           
                                        hpa-analyzer v1.0.0                                         
====================================================================================================
Target directory : /Users/techdesigns/dev/hpa_analyzer/fixtures/bad-chart
Generated        : 2026-07-18 00:29:42
Chart            : legacy-payments v1.0 (appVersion ?)

Files analyzed:
  chart      : Chart.yaml
  values     : values-prod.yaml
  values     : values.yaml
  template   : templates/deployment.yaml
  template   : templates/hpa.yaml
  template   : templates/service.yaml
  dockerfile : Dockerfile  [Java 8u151, jdk]


====================================================================================================
1. EXECUTIVE SUMMARY
====================================================================================================

  OVERALL QUALITY SCORE :  45.5 / 100   GRADE: F
  [#######################...........................]
  Analysis mode         : static

  Findings: 11 critical, 11 high, 14 medium, 16 low, 8 info

  Fix these first (each is an outage or a dead feature, not a style issue):
    1. [HP004] HPA minReplicas > maxReplicas (invalid)  (templates/hpa.yaml)   [overlay
    values-prod.yaml only]
    2. [HP025] HPA scales on memory for a JVM workload   [ASSUMED - verify before acting]
    3. [HP050] Deployment sets spec.replicas while an HPA manages it
    (templates/deployment.yaml)
    4. [RS002] Memory quantity uses 'm' (MILLI-bytes)  (values.yaml)
    5. [RS006] CPU limit below CPU request (invalid)  (templates/deployment.yaml)   [overlay
    values-prod.yaml only]
    ... and 6 more critical finding(s) - see the full list in section 4.


====================================================================================================
2. ANALYSIS COVERAGE - WHAT WAS AND WAS NOT CHECKED
====================================================================================================

Findings can only come from files that were successfully analyzed. Anything marked as failed,
skipped or unknown below produced NO findings - treat that as missing coverage, never as a clean
bill of health.

+---------------------------+-----------------------------------------------------------+
| Input                     | Coverage                                                  |
+===========================+===========================================================+
| values.yaml               | base values - used for primary analysis                   |
+---------------------------+-----------------------------------------------------------+
| values-prod.yaml          | overlay values - analyzed as a separate variant           |
+---------------------------+-----------------------------------------------------------+
| templates/deployment.yaml | statically parsed (1 object(s))                           |
+---------------------------+-----------------------------------------------------------+
| templates/hpa.yaml        | statically parsed (1 object(s))                           |
+---------------------------+-----------------------------------------------------------+
| templates/service.yaml    | statically parsed (1 object(s))                           |
+---------------------------+-----------------------------------------------------------+
| Dockerfile                | parsed (Java 8u151 detected)                              |
+---------------------------+-----------------------------------------------------------+
| values-prod.yaml          | variant analyzed - 4 additional finding(s) vs base values |
+---------------------------+-----------------------------------------------------------+

Mode: static. Static scrubbing approximates helm rendering: conditionals are analyzed as taken and
complex template expressions may hide configuration from these checks. Install helm on PATH and
re-run for rendered-truth analysis - it materially improves precision.


====================================================================================================
3. SCORECARD BY CATEGORY
====================================================================================================

+----------------------------------------+-------+-------+--------+--------------------+
| Category                               | Score | Grade | Weight | Findings (C/H/M/L) |
+========================================+=======+=======+========+====================+
| Helm Chart Structure & Hygiene         |  49.0 | F     | 4      | 2H, 2M, 5L         |
+----------------------------------------+-------+-------+--------+--------------------+
| Kubernetes Templates & API Versions    |  35.0 | F     | 5      | 2C, 1M, 3L         |
+----------------------------------------+-------+-------+--------+--------------------+
| Resource Requests & Limits             |  32.0 | F     | 15     | 2C, 1H, 1M         |
+----------------------------------------+-------+-------+--------+--------------------+
| Horizontal Pod Autoscaling             |  20.0 | F     | 15     | 3C, 1H, 2L         |
+----------------------------------------+-------+-------+--------+--------------------+
| Availability & Disruption Tolerance    |  79.0 | C+    | 8      | 3M, 1L             |
+----------------------------------------+-------+-------+--------+--------------------+
| Health Probes & Lifecycle              |  70.0 | C-    | 10     | 1H, 3M             |
+----------------------------------------+-------+-------+--------+--------------------+
| Dockerfile Quality                     |  66.0 | D     | 8      | 1C, 1M, 1L         |
+----------------------------------------+-------+-------+--------+--------------------+
| Java / JVM Container Fitness           |  21.0 | F     | 14     | 1C, 3H, 2M, 2L     |
+----------------------------------------+-------+-------+--------+--------------------+
| Security Posture                       |  52.0 | F     | 7      | 3H, 1M, 2L         |
+----------------------------------------+-------+-------+--------+--------------------+
| Cross-File Consistency (Chart <-> JVM) |  63.0 | D     | 14     | 2C                 |
+----------------------------------------+-------+-------+--------+--------------------+

Scoring model: each category starts at 100; deductions are CRITICAL -25, HIGH -12, MEDIUM -6, LOW
-3, INFO -0, floored at 0. Overall = weighted mean over applicable categories (N/A categories are
excluded, not free points).


====================================================================================================
4. FINDINGS AND REMEDIATION
====================================================================================================


----------------------------------------------------------------------------------------------------
CRITICAL  (11)
----------------------------------------------------------------------------------------------------

[HP004] HPA minReplicas > maxReplicas (invalid)
    Category: Horizontal Pod Autoscaling | templates/hpa.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : [with values overlay values-prod.yaml] HPA 'RELEASE-NAME-payments': minReplicas=25 >
    maxReplicas=20.
    Why   : Invalid object - the API server rejects it, so the workload has NO autoscaling and
    helm upgrade fails.
    Math  : Constraint violated: 25 <= 20 is false.
    Fix   : Make min <= max.

[HP025] HPA scales on memory for a JVM workload
    Category: Horizontal Pod Autoscaling
    Basis : ASSUMED - the tool could not observe this directly and fell back to a guess; verify
    before acting (see Assumes).
    Assumes: the workload this HPA targets is the Java service (inferred from a single workload
    + a Java Dockerfile; the HPA's target was not resolvable by name) - if that is wrong, this
    finding does not apply.
    Found : HPA 'RELEASE-NAME-payments' includes a memory utilization/average-value metric
    (target 80%).
    Why   : Memory-utilization HPA assumes memory falls when load falls. A JVM violates that
    assumption: committed heap stays at or near its high-water mark (most collectors return
    memory to the OS reluctantly or never), so measured 'utilization' stays high after load
    drops. Result: the HPA scales UP under load but never scales back DOWN - it ratchets to
    maxReplicas and stays there.
    Math  : Example with Xmx-bound JVM: after warmup RSS ~= heap_committed + off-heap ~=
    constant C. utilization = C/request stays > 80% regardless of traffic => desired =
    ceil(current x util/80) >= current forever (HPA tolerance +/-10%). Scale-down condition util
    < 80% is unreachable.
    Fix   : Scale JVMs on CPU or on a load-proportional metric (requests/sec via external
    metrics, queue depth). If memory must gate scaling, use it only as a secondary metric and
    remember HPA takes the MAX of all metric proposals - memory will dominate.

[HP050] Deployment sets spec.replicas while an HPA manages it
    Category: Horizontal Pod Autoscaling | templates/deployment.yaml:7
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments' sets spec.replicas=2 with no guard at all, AND an
    HPA targets this workload.
    Why   : Helm and the HPA now fight over the same field. Every 'helm upgrade' resets replicas
    to the template value; if the HPA had scaled to 10 and the template says 2, the deploy
    instantly kills 8 loaded pods mid-traffic. The HPA scales back up minutes later - after your
    users noticed.
    Math  : Upgrade at peak: pods 10 -> 2 (helm apply) => per-pod load x5 instantly; if pods
    saturate at ~2x, the service is down until HPA re-adds pods (>= 1 metric period + JVM
    startup).
    Fix   : Omit spec.replicas entirely when autoscaling is enabled:
            {{- if not .Values.autoscaling.enabled }}
            replicas: {{ .Values.replicaCount }}
            {{- end }}

[RS002] Memory quantity uses 'm' (MILLI-bytes)
    Category: Resource Requests & Limits | values.yaml:15
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments', container 'payments': requests.memory = '512m'.
    Why   : Lowercase 'm' is a legal Kubernetes suffix meaning 1/1000 of a byte. '512m' is 0.512
    BYTES, not 512 MiB. As a request it schedules the pod as if it needs nothing; as a limit the
    container is OOM-killed on its first allocation.
    Math  : '512m' = 0.512 bytes; '512Mi' = 536,870,912 bytes - a factor of 1000 x 1024^2 ~=
    1.05 billion.
    Fix   : Use '512Mi'.

[RS006] CPU limit below CPU request (invalid)
    Category: Resource Requests & Limits | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : [with values overlay values-prod.yaml] Deployment 'RELEASE-NAME-payments', container
    'payments': requests.cpu=2 > limits.cpu=1.
    Why   : Kubernetes requires limit >= request; the API server rejects this pod spec and the
    Deployment can never roll out.
    Math  : 1 core (limit) < 2 cores (request).
    Fix   : Raise the limit or lower the request.

[JV011] Java 8u151: only experimental cgroup support
    Category: Java / JVM Container Fitness | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : 8u131-8u190 need -XX:+UnlockExperimentalVMOptions -XX:+UseCGroupMemoryLimitForHeap;
    flags are ABSENT.
    Why   : Without the experimental flags applied, this JVM sizes its heap from host RAM
    exactly like pre-8u131. Even with them, CPU limits are ignored (GC/JIT thread pools sized
    for the node) and MaxRAMPercentage does not exist.
    Fix   : Upgrade to 8u191+ where -XX:+UseContainerSupport is on by default, then remove the
    experimental flags (they are removed in Java 11 and abort startup there).

[XF001] Max heap >= container memory limit
    Category: Cross-File Consistency (Chart <-> JVM) | templates/deployment.yaml
    Basis : ASSUMED - the tool could not observe this directly and fell back to a guess; verify
    before acting (see Assumes).
    Assumes: the node has ~16 GiB RAM (used only because this JVM cannot see the container
    limit) - if that is wrong, this finding does not apply.
    Found : [with values overlay values-prod.yaml] Deployment 'RELEASE-NAME-payments' /
    container 'payments': effective max heap 4 GiB (JVM CANNOT see the limit (8u151 without
    applied experimental cgroup flags); default 25% x assumed node RAM 16 GiB) vs limits.memory
    4 GiB.
    Why   : The heap ALONE meets or exceeds the limit before counting metaspace, stacks and
    buffers. The kernel will OOM-kill the container (exit 137, no Java stack trace, no heap
    dump) as soon as the heap fills - typically under first real load.
    Math  : H(4 GiB) >= L(4 GiB); required: H + ~404 MiB (non-heap) <= L.
    Fix   : Heap <= 50-75% of the limit. Either raise limits.memory or lower
    -Xmx/MaxRAMPercentage.

[XF003] JVM cannot see the container memory limit
    Category: Cross-File Consistency (Chart <-> JVM) | Dockerfile
    Basis : DERIVED - arithmetic on your values using the stated model and estimated constants;
    re-check with measured numbers.
    Found : [with values overlay values-prod.yaml] 8u151 without applied experimental cgroup
    flags; the chart sets limits.memory=4 GiB but the JVM sizes itself from the node.
    Why   : Limits only constrain (kill); they do not inform an unaware JVM. The JVM aims for a
    heap derived from NODE RAM and is OOM-killed at the container limit it never knew about.
    Math  : Default heap = node_RAM/4 = 4 GiB (assumed 16 GiB node) vs limit 4 GiB => kill at
    100% of the JVM's target.
    Fix   : Upgrade the JDK (8u191+/11.0.16+/17+) or set explicit -Xmx.

[DF013] JAVA_OPTS is defined but NEVER applied
    Category: Dockerfile Quality | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : ENV sets JAVA_OPTS, but the container's launch command never references it and the
    JVM does not read it by itself. Inert flags: -Dspring.profiles.active=prod
    -XX:MaxPermSize=256m -Xms512m -Xmx3g.
    Why   : JAVA_OPTS is only a convention - nothing reads it automatically. Every flag in it
    (heap sizing, GC, container tuning) is silently NOT in effect; the JVM runs on pure defaults
    while everyone believes it is tuned. All other findings in this report judge the options the
    JVM actually receives.
    Fix   : Either switch to JAVA_TOOL_OPTIONS (the JVM itself reads that env var) or expand it
    via 'ENTRYPOINT ["sh","-c","exec java $JAVA_OPTS -jar app.jar"]'.

[TP010] Deprecated/removed apiVersion for Deployment
    Category: Kubernetes Templates & API Versions | templates/deployment.yaml:1
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments' uses apiVersion apps/v1beta2, removed in
    Kubernetes 1.16.
    Why   : On clusters >= 1.16 the API server rejects this object outright: helm upgrade fails
    and the release can get stuck (existing release manifests reference dead APIs).
    Fix   : Move to apps/v1 and adjust the spec to the new schema.

[TP010] Deprecated/removed apiVersion for HorizontalPodAutoscaler
    Category: Kubernetes Templates & API Versions | templates/hpa.yaml:2
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : HorizontalPodAutoscaler 'RELEASE-NAME-payments' uses apiVersion autoscaling/v2beta1,
    removed in Kubernetes 1.25.
    Why   : On clusters >= 1.25 the API server rejects this object outright: helm upgrade fails
    and the release can get stuck (existing release manifests reference dead APIs).
    Fix   : Move to autoscaling/v2 and adjust the spec to the new schema.

----------------------------------------------------------------------------------------------------
HIGH  (11)
----------------------------------------------------------------------------------------------------

[HP023] HPA CPU target 95% leaves no scaling headroom
    Category: Horizontal Pod Autoscaling | templates/hpa.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : HPA 'RELEASE-NAME-payments' targets averageUtilization=95%.
    Why   : Scaling is not instantaneous: metrics lag (15-60s), pods schedule, images pull, and
    a JVM warms up. During that window existing pods must absorb the excess ABOVE the target. At
    95% the buffer before saturation is only 5%.
    Math  : Absorbable load growth while scaling = 100/95 - 1 = 5%. With a 90s JVM startup, load
    growing faster than 5% per ~2 min saturates pods before help arrives.
    Fix   : Target 60-75% for JVM services; lower if startup is slow.

[RS005] No memory limit
    Category: Resource Requests & Limits | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments', container 'payments': limits.memory is not set.
    Why   : Memory is incompressible: when a node runs out, the kernel OOM-killer picks victims.
    An unlimited JVM with a native leak or unbounded -Xmx can evict every other pod on the node.
    For JVM workloads a memory limit is the container's contract with the JVM's
    container-awareness (MaxRAMPercentage is computed FROM it).
    Fix   : Set limits.memory (commonly = requests.memory for JVMs, giving Guaranteed-style
    memory QoS).

[JV001] Java 8 runtime
    Category: Java / JVM Container Fitness | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Base image openjdk:8u151-jdk is Java 8 (update 151).
    Why   : Java 8 (2014) predates containers: its container support is a chain of backports
    with sharp edges (see the related findings), it lacks modern collectors (G1 improvements,
    ZGC/Shenandoah), compact strings (-10-20% heap for string-heavy apps), TLS 1.3 defaults and
    years of JIT gains. Every JVM<->container problem this report checks for is worse on 8.
    Fix   : Plan a migration to 17 or 21 (LTS). Typical first step: run on a current 8 update
    (8u392+) while testing 17.

[JV013] Java 8u151: blind to cgroup v2 nodes
    Category: Java / JVM Container Fitness | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : cgroup v2 support reached Java 8 at 8u372.
    Why   : Modern node OSes (Ubuntu >= 22.04, EKS AL2023, GKE COS, anything K8s ~1.25+) default
    to cgroup v2. On such nodes this JVM finds no cgroup v1 files, concludes it is NOT in a
    container, and reverts to host-RAM/host-CPU sizing - re-introducing the OOM-kill behavior of
    pre-container-aware JVMs even though UseContainerSupport is 'on'.
    Fix   : Upgrade to 8u372+ / 11.0.16+ / 17+, or pin explicit -Xmx and
    -XX:ActiveProcessorCount as a stopgap.

[JV021] No JVM heap sizing is actually applied
    Category: Java / JVM Container Fitness | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Neither -Xmx nor -XX:MaxRAMPercentage/-XX:MaxRAMFraction is applied. Heap sizing
    flags DO exist in an env var, but nothing applies them (see DF013) - the JVM runs on pure
    defaults.
    Why   : Without an applied heap bound the JVM uses its ergonomic default: 25% of the memory
    it can see. Two consequences: (a) most of the container memory you pay for is never used for
    heap, and (b) if the JVM cannot see the limit (old JDK, cgroup v2 mismatch - see other
    findings) that 25% is 25% of the NODE.
    Math  : Default heap = 0.25 x visible_RAM. Container limit 1 GiB => heap 256 MiB (why so
    small?). No visible limit on a 64 GiB node => heap 16 GiB (why so large?) -> OOMKill.
    Fix   : Apply -XX:MaxRAMPercentage=50-75 (container-aware JDKs) or an explicit -Xmx ~ 50-75%
    of limits.memory, via a mechanism that actually reaches the JVM (JAVA_TOOL_OPTIONS, or
    exec-form expansion).

[PB004] Liveness can kill a still-starting JVM
    Category: Health Probes & Lifecycle | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments', container 'payments': no startupProbe and
    liveness initialDelaySeconds=5s.
    Why   : JVM apps routinely need 30-120s+ to start (class loading, JIT, Spring context,
    connection pools) - especially with small CPU requests. If liveness fires before the app can
    answer, kubelet kills it, the restart is even slower (cold cache + CPU contention), and the
    pod enters CrashLoopBackOff without ever being unhealthy.
    Math  : Worst-case time-to-kill = initialDelay(5) + period(5) x failureThreshold(3) = 20s. A
    JVM needing more than 20s to start NEVER becomes live.
    Fix   : Add a startupProbe (e.g. periodSeconds=5, failureThreshold=36 -> up to 180s grace) -
    liveness only begins after startup succeeds.

[DF020] Container runs as root (no USER instruction)
    Category: Security Posture | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : No non-root USER is set in the final stage.
    Why   : Defense in depth: a compromised JVM running as root can write anywhere in the
    container and is one kernel bug from the node. Pod Security 'Restricted' will refuse the pod
    unless runAsNonRoot is satisfied - and an image built for root often fails when forced
    non-root at deploy time.
    Fix   : Create a user in the image and set USER (most Temurin images: no default user - add
    one).

[DF021] Possible secret baked into image
    Category: Security Posture | Dockerfile:4
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Line 4: ENV DB_PASSWORD=changeme123
    Why   : ENV/ARG values are stored in image layers and visible via 'docker history' to anyone
    who can pull the image.
    Fix   : Inject secrets at runtime (K8s Secrets -> env/volume), never at build time.

[SC001] Container may run as root
    Category: Security Posture | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments', container 'payments': neither runAsNonRoot: true
    nor a non-zero runAsUser is set.
    Why   : A container escape from a root process is a node compromise. 'Restricted' Pod
    Security Standard requires runAsNonRoot; clusters enforcing it will refuse this pod.
    Fix   : Set securityContext.runAsNonRoot: true (+ runAsUser) and add USER in the Dockerfile.

[PA001] Parse problem
    Category: Helm Chart Structure & Hygiene | values.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : values.yaml: duplicate key 'resources' at line 27 (later value silently wins)
    Why   : Files that cannot be parsed (or contain duplicate keys, where the last value
    silently wins) hide their real configuration from both this analyzer and from humans
    reviewing the chart.
    Fix   : Fix the syntax; run 'helm template' and a YAML linter in CI.

[VA002] Image tag is 'latest' or empty
    Category: Helm Chart Structure & Hygiene | values.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : image.tag is 'latest'.
    Why   : 'latest' is mutable: two pods of the same ReplicaSet can run different builds after
    a node pulls a newer image; rollbacks become impossible because the tag no longer identifies
    a build. With imagePullPolicy:Always a registry outage blocks pod restarts.
    Fix   : Pin an immutable tag (version or git SHA), ideally defaulting to .Chart.AppVersion;
    use digests for maximum reproducibility.

----------------------------------------------------------------------------------------------------
MEDIUM  (14)
----------------------------------------------------------------------------------------------------

[RS010] CPU request likely too small for a JVM
    Category: Resource Requests & Limits | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments', container 'payments': requests.cpu=100m.
    Why   : JVM startup (class loading + JIT) is CPU-hungry. Under node contention a pod is
    guaranteed only its request; at 100m a Spring-style app can take minutes to become ready and
    trip liveness probes.
    Fix   : Request >= 250-500m for JVM services; consider a startupProbe.

[JV017] PermGen flag on Java 8+ (ignored)
    Category: Java / JVM Container Fitness | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : -XX:MaxPermSize=256m - PermGen was removed in Java 8. [currently INERT - defined but
    never applied; see DF013]
    Why   : The flag is silently ignored (with a warning). Whoever set it believes class
    metadata is capped; it is NOT - Metaspace grows unbounded unless MaxMetaspaceSize is set,
    eating into the container limit.
    Fix   : Replace with -XX:MaxMetaspaceSize=... if a cap is desired.

[JV026] No applied -XX:+ExitOnOutOfMemoryError
    Category: Java / JVM Container Fitness | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : The flag is absent from the options the JVM will actually receive.
    Why   : After a heap OutOfMemoryError a JVM often limps on with dead threads and corrupted
    state - alive enough to pass a TCP liveness probe, broken enough to fail every request.
    Kubernetes can only heal what dies.
    Fix   : Add -XX:+ExitOnOutOfMemoryError (and optionally -XX:+HeapDumpOnOutOfMemoryError with
    a mounted dump path).

[LC001] Short terminationGracePeriodSeconds
    Category: Health Probes & Lifecycle | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments': terminationGracePeriodSeconds=10s.
    Why   : On SIGTERM a JVM must stop accepting traffic, drain in-flight requests, close pools
    and (often) deregister from discovery. 10s is rarely enough; kubelet then SIGKILLs
    mid-request.
    Fix   : Use >= 30s (default) and ensure the app handles SIGTERM (see Dockerfile ENTRYPOINT
    findings).

[PB003] Liveness and readiness probes are identical
    Category: Health Probes & Lifecycle | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments', container 'payments': same endpoint AND same
    timings.
    Why   : Readiness failure should shed traffic (recoverable); liveness failure RESTARTS the
    pod. Identical probes turn every transient slowdown (GC pause, downstream hiccup) into a
    restart storm - and restarts of a slow-starting JVM amplify the outage.
    Fix   : Make liveness strictly more tolerant: longer period, higher failureThreshold, and an
    endpoint that does NOT check downstream dependencies.

[PB005] Probe timeoutSeconds = 1 (default) is fragile for JVMs
    Category: Health Probes & Lifecycle | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments', container 'payments': livenessProbe
    timeoutSeconds=1s.
    Why   : A single stop-the-world GC pause > 1s fails the probe. Three in a row (default
    failureThreshold=3) restarts a healthy pod under exactly the load that caused the GC
    pressure - positive feedback into an outage.
    Fix   : Set timeoutSeconds to 3-5s for JVM services.

[AV002] Deployment strategy Recreate
    Category: Availability & Disruption Tolerance | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments': strategy.type=Recreate.
    Why   : Recreate deletes ALL old pods before starting new ones - guaranteed downtime on
    every deploy, magnified by JVM startup time.
    Fix   : Use RollingUpdate unless the app truly cannot run two versions concurrently.

[AV010] No PodDisruptionBudget
    Category: Availability & Disruption Tolerance
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Chart ships Deployments/StatefulSets but no PDB.
    Why   : Without a PDB, voluntary disruptions (node drains, cluster upgrades, spot reclaims
    via drain) may evict ALL replicas simultaneously. With an HPA this is worse:
    cluster-autoscaler consolidation happily drains nodes hosting every replica.
    Fix   : Add a PDB with maxUnavailable: 1 (or 25%) selecting the same pods.

[DF022] ADD from URL
    Category: Dockerfile Quality | Dockerfile:6
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Line 6: ADD https://internal.example.com/tools/agent.jar /opt/agent.jar
    Why   : ADD-from-URL is unverified (no checksum), uncached and non-reproducible.
    Fix   : Use curl with checksum verification in a RUN, or COPY a vendored artifact.

[HP006] HPA minReplicas=1
    Category: Availability & Disruption Tolerance | templates/hpa.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : HPA 'RELEASE-NAME-payments': minReplicas=1.
    Why   : At quiet times the HPA scales to a single pod: every voluntary disruption or crash
    at min-scale is then a full outage, and recovery requires a cold JVM start under rising
    load.
    Fix   : minReplicas: 2 for anything user-facing.

[SC002] allowPrivilegeEscalation not disabled
    Category: Security Posture | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments', container 'payments': allowPrivilegeEscalation
    is not set to false.
    Why   : Defaults to true; permits setuid binaries to gain privileges. Required false by the
    Restricted PSS profile.
    Fix   : Set securityContext.allowPrivilegeEscalation: false.

[TP002] Hardcoded namespace in template
    Category: Kubernetes Templates & API Versions | templates/deployment.yaml:5
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : namespace: production is hardcoded.
    Why   : Hardcoding the namespace breaks 'helm install -n <ns>' and makes multi-env installs
    collide.
    Fix   : Omit metadata.namespace (Helm applies the release namespace) or use {{
    .Release.Namespace }}.

[CH002] Chart apiVersion v1 (Helm 2 era)
    Category: Helm Chart Structure & Hygiene | Chart.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Chart.yaml declares apiVersion: v1.
    Why   : apiVersion v1 targets Helm 2, which reached end of life in Nov 2020 (no security
    fixes). v2 charts support dependencies in Chart.yaml and library charts.
    Fix   : Set apiVersion: v2 and manage dependencies via the 'dependencies' key instead of
    requirements.yaml.

[CH006] Chart version is not SemVer
    Category: Helm Chart Structure & Hygiene | Chart.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : version: 1.0 does not match MAJOR.MINOR.PATCH.
    Why   : helm package/repo tooling assumes SemVer; sorting and constraint matching (e.g. in
    Chart dependencies) break otherwise.
    Fix   : Use a SemVer 2 version like 1.4.2.

----------------------------------------------------------------------------------------------------
LOW  (16)
----------------------------------------------------------------------------------------------------

[HP007] Very wide HPA range
    Category: Horizontal Pod Autoscaling | templates/hpa.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : HPA 'RELEASE-NAME-payments': min=1, max=20 (20x).
    Why   : A 10x+ band usually means requests were never right-sized; check the cluster can
    actually schedule max pods (max x request <= cluster headroom) or scaling silently stops at
    Pending pods.
    Fix   : Verify capacity: maxReplicas x cpu request and x memory request.

[HP030] No HPA behavior block (defaults apply)
    Category: Horizontal Pod Autoscaling | templates/hpa.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : HPA 'RELEASE-NAME-payments' relies on default scaling behavior.
    Why   : Defaults: scale-up may double pods every 15s (or +4 pods, whichever is larger) with
    0s stabilization; scale-down waits 300s. For slow-starting JVMs an aggressive default
    scale-up adds many cold pods that all JIT-compile at once (CPU burst -> even higher
    utilization -> more scaling: a warmup storm).
    Fix   : Add behavior.scaleUp with a stabilizationWindowSeconds of 60-120s and/or policies
    limiting pods-per-minute; keep scale-down conservative.

[JV024] Xms != Xmx in a container
    Category: Java / JVM Container Fitness | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : -Xms 512 MiB vs -Xmx 3 GiB. [currently INERT - defined but never applied; see DF013]
    Why   : The pod's memory REQUEST must cover Xmx anyway (the heap will get there under load
    and memory is rarely returned). Growing the heap in steps just adds early GC cycles and
    page-fault latency; you gain nothing you can bin-pack on.
    Fix   : Set Xms = Xmx (or AlwaysPreTouch for latency-critical pods).

[JV027] GC not pinned - ergonomics may pick SerialGC in-container
    Category: Java / JVM Container Fitness | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Java 8 with no applied collector flag.
    Why   : JVM ergonomics picks the collector from visible CPUs/RAM: with <2 CPUs or <~1792 MiB
    visible, you get SerialGC - single-threaded, long pauses - and a CPU limit of 1000m means
    exactly that. Java 8's default (Parallel) also differs from 9+ (G1): upgrades silently
    change GC behavior.
    Fix   : Pin the collector explicitly (-XX:+UseG1GC for most services).

[AV003] No pod anti-affinity / topology spread
    Category: Availability & Disruption Tolerance | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments': neither podAntiAffinity nor
    topologySpreadConstraints set.
    Why   : Multiple replicas may all land on ONE node; a single node failure then behaves
    exactly like replicas=1. Scaling out via HPA without spreading also concentrates the new
    load.
    Fix   : Add topologySpreadConstraints on kubernetes.io/hostname (and zone) with maxSkew: 1.

[DF004] Full JDK in final image (no multi-stage build)
    Category: Dockerfile Quality | Dockerfile
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Final image openjdk:8u151-jdk is a JDK and the Dockerfile has a single stage.
    Why   : A JDK ships compilers and tools the service never uses: bigger attack surface,
    bigger pulls (slower cold starts - which matters when the HPA is adding pods under load).
    Fix   : Multi-stage: build with the JDK image, run on the matching -jre (or distroless/java)
    image.

[SC003] Linux capabilities not dropped
    Category: Security Posture | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments', container 'payments': capabilities.drop does not
    include ALL.
    Why   : Default capability set includes NET_RAW etc.; a Java service needs none of them.
    Fix   : capabilities: { drop: [ALL] }.

[SC004] Root filesystem writable
    Category: Security Posture | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments', container 'payments': readOnlyRootFilesystem not
    true.
    Why   : Read-only rootfs blocks attackers dropping tools/persisting; JVMs typically only
    need /tmp (mount an emptyDir).
    Fix   : Set readOnlyRootFilesystem: true and mount emptyDir at /tmp (add -Djava.io.tmpdir if
    needed).

[TP012] Deployment has no labels at all
    Category: Kubernetes Templates & API Versions | templates/deployment.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Deployment 'RELEASE-NAME-payments' defines no metadata.labels.
    Why   : Unlabeled objects are invisible to selectors and monitoring.
    Fix   : Add the standard app.kubernetes.io/* label set.

[TP012] HorizontalPodAutoscaler has no labels at all
    Category: Kubernetes Templates & API Versions | templates/hpa.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : HorizontalPodAutoscaler 'RELEASE-NAME-payments' defines no metadata.labels.
    Why   : Unlabeled objects are invisible to selectors and monitoring.
    Fix   : Add the standard app.kubernetes.io/* label set.

[TP012] Service has no labels at all
    Category: Kubernetes Templates & API Versions | templates/service.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : Service 'RELEASE-NAME-payments' defines no metadata.labels.
    Why   : Unlabeled objects are invisible to selectors and monitoring.
    Fix   : Add the standard app.kubernetes.io/* label set.

[CH007] No appVersion in Chart.yaml
    Category: Helm Chart Structure & Hygiene | Chart.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : appVersion is not set.
    Why   : appVersion documents which application build the chart deploys and is the
    conventional default for the image tag (.Chart.AppVersion). Without it, image tags tend to
    drift to 'latest'.
    Fix   : Set appVersion to the deployed application version and reference it as the default
    image tag.

[CH008] No chart description
    Category: Helm Chart Structure & Hygiene | Chart.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : description is not set.
    Why   : Descriptions surface in 'helm search' and chart repos; missing metadata makes the
    chart look unmaintained.
    Fix   : Add a one-line description.

[CH010] No kubeVersion constraint
    Category: Helm Chart Structure & Hygiene | Chart.yaml
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : kubeVersion is not set.
    Why   : Without a kubeVersion constraint the chart will happily install onto clusters whose
    APIs it does not support (e.g. autoscaling/v2 requires Kubernetes >= 1.23), failing at apply
    time instead of install time.
    Fix   : Add e.g. kubeVersion: ">=1.23.0-0".

[CH021] No _helpers.tpl
    Category: Helm Chart Structure & Hygiene
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : templates/_helpers.tpl not found.
    Why   : Name/label boilerplate gets duplicated per template without helpers, which is how
    label drift (and broken selectors) happens.
    Fix   : Add _helpers.tpl with <chart>.name, <chart>.fullname, <chart>.labels and
    <chart>.selectorLabels helpers.

[CH023] No values.schema.json
    Category: Helm Chart Structure & Hygiene
    Basis : OBSERVED - read directly from your files (stated as fact).
    Found : values.schema.json not found.
    Why   : Schema validation rejects typo'd or type-mismatched values at install time (e.g.
    memory: 512 instead of '512Mi') - exactly the class of error that breaks resources/HPA
    silently.
    Fix   : Add values.schema.json (JSON Schema) covering at least resources and autoscaling
    blocks.

----------------------------------------------------------------------------------------------------
INFO  (8)
----------------------------------------------------------------------------------------------------
  Housekeeping items - zero score impact, one line each so they stop competing with real problems:
    [DF025] No .dockerignore (Dockerfile) -> Add .dockerignore.
    [SC006] ServiceAccount token automounted (templates/deployment.yaml) -> Set
    automountServiceAccountToken: false on the pod spec (or SA).
    [TP003] TODO marker in template (templates/service.yaml) -> Resolve or ticket it.
    [CH009] No maintainers listed (Chart.yaml) -> Add maintainers with name/email.
    [CH011] No icon/home/sources metadata (Chart.yaml) -> Add links if the chart is published.
    [CH022] No NOTES.txt -> Add templates/NOTES.txt with connection/usage hints.
    [CH024] No .helmignore -> Add a .helmignore.
    [CH025] No helm test hooks -> Add a tests/ pod that curls the service readiness endpoint.


====================================================================================================
5. MATHEMATICAL PROOF TABLES
====================================================================================================

Every table below derives its verdict from arithmetic on values found in YOUR files (estimates are
labeled and conservative). Re-check any number by hand.

----------------------------------------------------------------------------------------------------
TABLE 1: JVM memory budget - Deployment 'RELEASE-NAME-payments' / container 'payments'
----------------------------------------------------------------------------------------------------
Container memory must hold the WHOLE JVM, not just the heap. Estimation model: T = H + Metaspace +
CodeCache + threads*Xss + DirectBuffers + GC/internal. JVM visibility of the limit: 8u151 without
applied experimental cgroup flags.

+-----------------------------+---------+----------------------------------------------------------+
| Component                   | Size    | Basis                                                    |
+=============================+=========+==========================================================+
| Container memory limit      | NOT SET | -                                                        |
+-----------------------------+---------+----------------------------------------------------------+
| Max heap (H)                | 4 GiB   | JVM CANNOT see the limit (8u151 without applied          |
|                             |         | experimental cgroup flags); default 25% x assumed node   |
|                             |         | RAM 16 GiB                                               |
+-----------------------------+---------+----------------------------------------------------------+
| Metaspace (est.)            | 128 MiB | typical framework app 80-180 MiB                         |
+-----------------------------+---------+----------------------------------------------------------+
| JIT code cache (est.)       | 64 MiB  | steady-state                                             |
+-----------------------------+---------+----------------------------------------------------------+
| Thread stacks (100 x 1 MiB) | 100 MiB | -Xss x thread count                                      |
+-----------------------------+---------+----------------------------------------------------------+
| Direct buffers (est.)       | 64 MiB  | est. 64 MiB typical                                      |
+-----------------------------+---------+----------------------------------------------------------+
| GC + JVM internal (est.)    | 48 MiB  | card tables, symbols                                     |
+-----------------------------+---------+----------------------------------------------------------+
| ESTIMATED PEAK RSS (T)      | 4.4 GiB | T = H + non-heap components                              |
+-----------------------------+---------+----------------------------------------------------------+

  VERDICT: No memory limit: the JVM competes with every pod on the node; a leak becomes the node's
  problem. Set a limit and size the heap from it.

----------------------------------------------------------------------------------------------------
TABLE 2: CPU as seen by the JVM - Deployment 'RELEASE-NAME-payments' / container 'payments'
----------------------------------------------------------------------------------------------------
Kubernetes CPU limits are CFS quotas; the JVM derives its parallelism from them.

+-----------------------+-------+------------------------------------------------------------------+
| Item                  | Value | Meaning                                                          |
+=======================+=======+==================================================================+
| CPU request           | 100m  | guaranteed share under contention; HPA denominator               |
+-----------------------+-------+------------------------------------------------------------------+
| CPU limit             | none  | pod may use idle node CPU                                        |
+-----------------------+-------+------------------------------------------------------------------+
| availableProcessors() | 1     | cpu.shares heuristic: ceil(request 100m / 1000m) - applies to    |
|                       |       | Java 8u151 (JDK 8 / pre-11.0.17). JDK 11.0.17+/17+ would instead |
|                       |       | see ALL node CPUs here.                                          |
+-----------------------+-------+------------------------------------------------------------------+
| Consequences          |       | sizes GC threads, ForkJoinPool, C2 compiler threads, Netty event |
|                       |       | loops                                                            |
+-----------------------+-------+------------------------------------------------------------------+

  VERDICT: No CPU limit: generally GOOD for JVMs (no CFS throttle); requests still guarantee fair
  share under contention.

----------------------------------------------------------------------------------------------------
TABLE 3: Probe budget vs JVM startup - Deployment 'RELEASE-NAME-payments' / 'payments'
----------------------------------------------------------------------------------------------------
A liveness probe that fires before the JVM can answer does not detect failure - it CAUSES it.

+------------------------------------+--------------+----------------------------------------------+
| Quantity                           | Value        | Note                                         |
+====================================+==============+==============================================+
| startupProbe window                | none         | liveness starts immediately                  |
+------------------------------------+--------------+----------------------------------------------+
| liveness initialDelaySeconds       | 5s           |                                              |
+------------------------------------+--------------+----------------------------------------------+
| liveness period x failureThreshold | 5s x 3 = 15s |                                              |
+------------------------------------+--------------+----------------------------------------------+
| Worst-case time-to-kill            | 20s          | init(5) + period(5) x failures(3)            |
+------------------------------------+--------------+----------------------------------------------+
| Typical JVM app startup            | ~60s         | mid-size Spring Boot on ~1 CPU (assumption;  |
|                                    |              | measure yours)                               |
+------------------------------------+--------------+----------------------------------------------+

  VERDICT: BUDGET 20s < startup ~60s: kubelet kills the pod BEFORE the app can come up; each
  restart is slower (CPU contention) => CrashLoopBackOff of a perfectly healthy build.

----------------------------------------------------------------------------------------------------
TABLE 4: QoS class and eviction order
----------------------------------------------------------------------------------------------------
QoS derivation: Guaranteed iff requests == limits for BOTH cpu and memory on every container;
BestEffort iff none set; else Burstable. Under node memory pressure kubelet evicts BestEffort, then
Burstable exceeding requests, then Guaranteed last.

+----------------------+---------+---------+---------+---------+-----------+-----------------------+
| Container            | req CPU | req Mem | lim CPU | lim Mem | QoS       | Eviction              |
+======================+=========+=========+=========+=========+===========+=======================+
| Deployment/RELEASE-N | 100m    | 0 B     | -       | -       | Burstable | evicted before        |
| AME-payments:payment |         |         |         |         |           | Guaranteed            |
| s                    |         |         |         |         |           |                       |
+----------------------+---------+---------+---------+---------+-----------+-----------------------+

  VERDICT: For latency-sensitive JVMs aim for Guaranteed memory (request = limit) and request-only
  CPU.

----------------------------------------------------------------------------------------------------
TABLE 5: HPA scaling arithmetic - HPA 'RELEASE-NAME-payments'
----------------------------------------------------------------------------------------------------
How the HPA converts measured CPU into replica counts (current replicas = 1 for illustration).

+-----------------+---------------+---------------------+-------------------+----------------------+
| Avg utilization | Per-pod usage | Raw formula         | Desired (clamped) | Action               |
+=================+===============+=====================+===================+======================+
| 48%             | 48m/pod       | ceil(1 x 48/95) = 1 | 1                 | no change            |
+-----------------+---------------+---------------------+-------------------+----------------------+
| 86%             | 86m/pod       | ceil(1 x 86/95) = 1 | 1                 | no change (within    |
|                 |               |                     |                   | 10% tolerance)       |
+-----------------+---------------+---------------------+-------------------+----------------------+
| 95%             | 95m/pod       | ceil(1 x 95/95) = 1 | 1                 | no change (within    |
|                 |               |                     |                   | 10% tolerance)       |
+-----------------+---------------+---------------------+-------------------+----------------------+
| 105%            | 105m/pod      | ceil(1 x 105/95) =  | 2                 | scale UP             |
|                 |               | 2                   |                   |                      |
+-----------------+---------------+---------------------+-------------------+----------------------+
| 142%            | 142m/pod      | ceil(1 x 142/95) =  | 2                 | scale UP             |
|                 |               | 2                   |                   |                      |
+-----------------+---------------+---------------------+-------------------+----------------------+
| 190%            | 190m/pod      | ceil(1 x 190/95) =  | 2                 | scale UP             |
|                 |               | 2                   |                   |                      |
+-----------------+---------------+---------------------+-------------------+----------------------+
| 285%            | 285m/pod      | ceil(1 x 285/95) =  | 3                 | scale UP             |
|                 |               | 3                   |                   |                      |
+-----------------+---------------+---------------------+-------------------+----------------------+

  VERDICT: Formula: desired = ceil(current x currentUtil / target), clamped to [1, 20], with a
  +/-10% tolerance dead-band. With cpu request 100m and target 95%, scale-out begins once average
  usage exceeds ~104m per pod (95% x 100m x 1.1).

----------------------------------------------------------------------------------------------------
TABLE 6: Availability vs replica floor
----------------------------------------------------------------------------------------------------
Redundancy math for the minimum number of pods the chart allows to exist.

+------------+------------------------+-------------------------------------+
| Replicas n | Availability 1-(1-p)^n | Expected downtime                   |
+============+========================+=====================================+
| 1          | 99.500000%             | ~216.0 min/month  <-- current floor |
+------------+------------------------+-------------------------------------+
| 2          | 99.997500%             | ~1.1 min/month                      |
+------------+------------------------+-------------------------------------+
| 3          | 99.999988%             | ~0.32 sec/month                     |
+------------+------------------------+-------------------------------------+
| 5          | 100.000000%            | ~0.00 sec/month                     |
+------------+------------------------+-------------------------------------+

  VERDICT: Assuming INDEPENDENT pod failures at 99.5% per-pod availability, availability =
  1-(1-p)^n. Your scale floor is n=1 (HPA minReplicas). n=1 concentrates ~100x more downtime than
  n=2 - redundancy, not pod quality, dominates availability. Caveat: the independence assumption
  is exactly what fails in correlated events (bad deploy, node/zone outage, shared dependency
  down) - those hit ALL replicas at once and no exponent helps. Treat this table as the upper
  bound that redundancy alone can buy.


====================================================================================================
6. EDUCATION APPENDIX - WHY THIS MATH MATTERS
====================================================================================================


6.1  THE HPA CONTROL LOOP

  Every ~15s the HPA computes, per metric:  desired = ceil(currentReplicas x currentValue /
  targetValue), takes the MAX across metrics, applies a +/-10% tolerance dead-band, clamps to
  [minReplicas, maxReplicas], then applies behavior policies (default: scale-up immediately,
  scale-down after a 300s stabilization window). Two facts follow directly from the formula:
      (a) CPU 'utilization' is usage divided by the pod's REQUEST - not its limit, not node
      capacity. Wrong requests make the HPA scale wrongly, in exact proportion.
      (b) Any metric that does not fall when replicas rise breaks the loop: desired can never
      go below current. JVM memory is the canonical example (heap high-water mark).

6.2  THE JVM MEMORY MODEL IN A CONTAINER

      container limit (cgroup memory.max)  <-- kernel kills here (exit 137)
      +--------------------------------------------------+
      |  heap (-Xmx / MaxRAMPercentage)                  |
      |  metaspace (classes; unbounded by default!)      |
      |  JIT code cache                                  |
      |  thread stacks (threads x -Xss, ~1 MiB each)     |
      |  direct/NIO buffers (default cap = Xmx!)         |
      |  GC bookkeeping, symbols, JVM itself             |
      +--------------------------------------------------+
  The kernel enforces the SUM. A heap that fits is necessary but not sufficient - rule of thumb:
  heap <= 50-75% of the limit, and never less than ~250-400 MiB of absolute non-heap headroom for
  framework apps. The kernel OOM kill (exit 137) produces NO Java stack trace and NO heap dump: if
  you see 137/OOMKilled with no OutOfMemoryError in logs, it was the cgroup, not the heap.

6.3  JAVA CONTAINER-AWARENESS TIMELINE

+-----------------------+---------------------------------+-----------------+----------------------+
| JVM                   | Sees cgroup v1 limits?          | Sees cgroup v2? | Heap % flags         |
+=======================+=================================+=================+======================+
| Java 8 < 8u131        | NO - uses host RAM/CPUs         | NO              | -Xmx only            |
+-----------------------+---------------------------------+-----------------+----------------------+
| 8u131 - 8u190         | memory only, with -XX:+UnlockEx | NO              | -Xmx, MaxRAMFraction |
|                       | perimentalVMOptions -XX:+UseCGr |                 |                      |
|                       | oupMemoryLimitForHeap           |                 |                      |
+-----------------------+---------------------------------+-----------------+----------------------+
| 8u191 - 8u371         | YES (UseContainerSupport        | NO              | MaxRAMPercentage     |
|                       | backport, default on)           |                 |                      |
+-----------------------+---------------------------------+-----------------+----------------------+
| 8u372+                | YES                             | YES             | MaxRAMPercentage     |
+-----------------------+---------------------------------+-----------------+----------------------+
| 11.0.0 - 11.0.15      | YES                             | NO              | MaxRAMPercentage     |
+-----------------------+---------------------------------+-----------------+----------------------+
| 11.0.16+, 15+, 17, 21 | YES                             | YES             | MaxRAMPercentage     |
+-----------------------+---------------------------------+-----------------+----------------------+
  cgroup v2 is the default on modern node images (Ubuntu 22.04+, EKS AL2023, current GKE/AKS). A
  pre-v2 JVM on a v2 node silently falls back to HOST sizing - the worst failure mode returns even
  though your JDK 'is container aware'.

6.4  A SANE BASELINE FOR A JVM SERVICE CHART

+--------------------------------+--------------------------------+--------------------------------+
| Setting                        | Baseline                       | Reason                         |
+================================+================================+================================+
| requests.memory =              | e.g. 1Gi / 1Gi                 | memory is incompressible;      |
| limits.memory                  |                                | Guaranteed-style memory QoS    |
+--------------------------------+--------------------------------+--------------------------------+
| requests.cpu                   | 250m-1000m ~ typical usage     | HPA denominator; scheduler     |
|                                |                                | packing                        |
+--------------------------------+--------------------------------+--------------------------------+
| limits.cpu                     | unset (or >= 2x request)       | avoid CFS throttling of GC/JIT |
|                                |                                | bursts                         |
+--------------------------------+--------------------------------+--------------------------------+
| -XX:MaxRAMPercentage           | 50-75                          | heap scales with the limit     |
+--------------------------------+--------------------------------+--------------------------------+
| -XX:+ExitOnOutOfMemoryError    | always                         | die visibly; let K8s heal      |
+--------------------------------+--------------------------------+--------------------------------+
| -XX:ActiveProcessorCount       | set if no cpu limit            | stable thread-pool sizing      |
+--------------------------------+--------------------------------+--------------------------------+
| startupProbe                   | period 5s x threshold 24-60    | protects slow JVM starts from  |
|                                |                                | liveness                       |
+--------------------------------+--------------------------------+--------------------------------+
| readiness != liveness          | always                         | shed traffic without restarts  |
+--------------------------------+--------------------------------+--------------------------------+
| HPA                            | autoscaling/v2, CPU 60-75%,    | headroom for scale-up lag      |
|                                | min >= 2                       |                                |
+--------------------------------+--------------------------------+--------------------------------+
| PDB                            | maxUnavailable: 1              | survive drains/upgrades        |
+--------------------------------+--------------------------------+--------------------------------+
| replicas                       | omit when HPA enabled          | helm upgrade must not reset    |
|                                |                                | scale                          |
+--------------------------------+--------------------------------+--------------------------------+
| ENTRYPOINT                     | exec form (java as PID 1)      | SIGTERM -> graceful shutdown   |
+--------------------------------+--------------------------------+--------------------------------+

6.5  THE RELATIVITY TRAP - REQUESTS, NOT LIMITS, DRIVE SCALING

  The HPA computes CPU 'utilization' as usage / REQUEST - never the limit, never node capacity.
  Set the request as a low placeholder and you lie to the controller. Example: request=100m,
  limit=2000m, actual usage=150m. The kernel sees a pod using 7.5% of its 2000m ceiling - nowhere
  near throttling. The HPA sees 150m/100m = 150% utilization and, against a 70% target, scales
  aggressively to maxReplicas while every pod sits nearly idle. Under-sized requests 'trick' the
  loop into believing the system is saturated when it is merely active. Right-size requests FIRST
  (a day of VPA in recommendation mode); accuracy in the denominator is the only route to
  stability in the output.

6.6  COMPRESSIBLE VS INCOMPRESSIBLE RESOURCES

+--------------------+----------------------------------------+------------------------------------+
|                    | CPU (compressible)                     | Memory (incompressible)            |
+====================+========================================+====================================+
| Exhaustion         | CFS throttling (slows down)            | OOM kill - SIGKILL (dies)          |
+--------------------+----------------------------------------+------------------------------------+
| Kernel signal      | container_cpu_cfs_throttled_periods_to | container_memory_working_set_bytes |
|                    | tal                                    |                                    |
+--------------------+----------------------------------------+------------------------------------+
| Tracks load?       | yes - roughly linear with work         | no - heap/baseline stays flat      |
+--------------------+----------------------------------------+------------------------------------+
| Good scale metric? | yes for stateless services             | NO - prone to permanent scale-out  |
+--------------------+----------------------------------------+------------------------------------+
  Because memory is incompressible and a JVM/Go runtime rarely returns it to the OS, memory
  'utilization' does not fall when load falls: an HPA on memory ratchets to max and stays.
  Diagnostic: if container_cpu_cfs_throttled_periods_total climbs while the HPA sits idle, your
  CPU request is too small or you are scaling on the wrong metric.

6.7  THE UNREADY-POD OUTAGE (dampening logic)

  When new pods fail readiness (bad deploy, missing DB schema) the HPA can REFUSE to scale even at
  100% CPU on the ready pods. Its four-step logic: (1) group pods Ready / Unready / Ignored; (2)
  first pass uses Ready pods only; (3) if that says scale-up, add the Unready pods back at 0%
  usage; (4) if the resulting ratio is within a +/-10% tolerance of target, do nothing. With
  maxSurge=100%, two hot pods plus two unready-at-0% average to (100+100+0+0)/4 = 50%, well inside
  the tolerance of a 70% target - so scaling STOPS exactly when you need it. Mitigation: keep
  maxSurge at 25-50% so the ready pods' signal survives the dampening.

6.8  THRASHING, STABILIZATION, AND THE HPA+VPA DEATH SPIRAL

  Thrashing: high metric -> scale up -> load per pod drops -> scale down -> load spikes -> repeat,
  churning pods and cold caches. Damp it with stabilizationWindowSeconds over a rolling history.
  The production pattern is ASYMMETRIC - react up fast, release down slowly:
    behavior:
      scaleUp:   { stabilizationWindowSeconds: 0,   policies: [{type: Percent, value: 100,
    periodSeconds: 15}] }
      scaleDown: { stabilizationWindowSeconds: 300, policies: [{type: Percent, value: 10,
    periodSeconds: 60}] }
  HPA+VPA on the SAME resource is a death spiral: VPA lowers the request because usage is low; the
  HPA sees a smaller denominator, computes higher utilization, and scales out unnecessarily. Rule:
  never let both auto-update the same resource. Use VPA in recommendation/initial mode to
  right-size requests; use the HPA to handle traffic. Also beware I/O-bound services: waiting on a
  lock or a slow downstream shows LOW CPU, so a CPU HPA never scales even as p99 breaches the SLO
  - scale those on latency or queue depth.

6.9  DEMAND METRICS - SCALE ON CAUSE, NOT SYMPTOM

  CPU/memory are lagging symptoms. Demand metrics track the cause:
+--------------------+--------------------------+--------------------------+
| Signal             | Best for                 | Target rule of thumb     |
+====================+==========================+==========================+
| Requests/sec (RPS) | stateless HTTP           | linear proxy for demand  |
+--------------------+--------------------------+--------------------------+
| Queue depth / lag  | workers (Kafka/RabbitMQ) | ~30s of work to process  |
+--------------------+--------------------------+--------------------------+
| p99 latency        | SLO-sensitive APIs       | p95 = 50% of the SLO     |
+--------------------+--------------------------+--------------------------+
| CPU utilization    | general compute          | 50-75% (60-75% for JVMs) |
+--------------------+--------------------------+--------------------------+
  Expose app metrics via the Prometheus Adapter, or drive scaling from external event sources with
  KEDA (queues, streams, cron). RPS scales linearly regardless of I/O wait, which is why it beats
  CPU for latency-bound services.

6.10  PREDICTIVE SCALING AND THE SCALING-INVARIANT SIGNAL

  Reactive scaling has a 'startup gap': the sync period (~15s) plus metrics-server scrape latency
  (30-60s) plus pod schedule + image pull + JVM warmup means the system is blind to the first
  minute of a surge. Predictive scaling forecasts load (e.g. Holt's / triple-exponential
  smoothing) and provisions capacity BEFORE the peak. It must forecast on a CLUSTER-WIDE AGGREGATE
  (sum of per-pod metrics), not a per-pod average: adding replicas drops the average even as total
  load rises, so the average is not a stable time series. The aggregate is scaling-invariant -
  stable however load redistributes - which is what makes clean extrapolation possible. A typical
  pipeline: align irregular batches onto a uniform grid, impute missing pods by carrying forward,
  gradually weight in new pods (so a starting pod is not read as a load 'drop'), forecast to the
  init-timeout horizon, then solve for the replica count needed now so capacity is ready when the
  peak arrives.

6.11  WORKLOAD -> SCALER DECISION FRAMEWORK

+----------------------+-------------------+------------------+------------------+
| Workload             | Signal            | Tool             | Target           |
+======================+===================+==================+==================+
| Stateless HTTP       | RPS / CPU         | HPA              | 50-60% CPU       |
+----------------------+-------------------+------------------+------------------+
| I/O-bound API        | p99 latency       | HPA + Prometheus | p95 = 50% of SLO |
+----------------------+-------------------+------------------+------------------+
| Message consumer     | queue depth / lag | KEDA             | ~30s of work     |
+----------------------+-------------------+------------------+------------------+
| Stateful / singleton | historical usage  | VPA (recommend)  | n/a              |
+----------------------+-------------------+------------------+------------------+
  Non-negotiables: minReplicas >= 2 (no single point of failure at scale-in); a
  PodDisruptionBudget so the HPA/drains cannot evict every pod at once; right-size requests before
  setting targets.

6.12  HOW TO READ THE 'BASIS' LINE ON EACH FINDING

  Every finding declares how the tool knows what it claims, so a guess never reads like a
  measurement: OBSERVED = read directly from your files (stated as fact); DERIVED = arithmetic on
  your values using the stated model and estimated non-heap constants (re-check with measured
  numbers); ASSUMED = the tool could NOT see the truth and fell back to a guess - it prints an
  'Assumes:' line, is flagged in the fix-first list, and (if CRITICAL) is capped at HIGH's score
  weight so the tool's own uncertainty can never sink your grade. When in doubt, act on OBSERVED
  first and verify ASSUMED before touching anything.

6.13  GOLDEN RULES

  1. The HPA divides by requests: requests ARE your scaling policy.
  2. The kernel sums everything: budget the whole JVM, not the heap.
  3. Kubernetes restarts what dies: make the JVM die on OOM, cleanly on SIGTERM.
  4. Never scale a JVM on memory utilization.
  5. Anything mutable ('latest', unpinned bases, helm-managed replicas under an HPA) will
  eventually mutate mid-incident.
  6. Scale up fast, scale down slow; never run HPA and VPA on the same resource.


====================================================================================================
7. METHODOLOGY AND LIMITATIONS
====================================================================================================

Analysis mode: static. Templates were parsed with Go-template expressions scrubbed and, where
possible, resolved from values.yaml, WITHOUT executing helm. Conditional blocks are analyzed as if
taken; complex expressions (tpl, printf, required, subcharts) are beyond static resolution and files
that failed to parse produced no findings at all - the coverage section lists every such gap.
Installing helm and re-running upgrades this report to rendered-truth analysis. Numeric estimates
(metaspace, thread counts, node sizes, startup times, per-pod availability) are stated inline and
should be replaced with your measured values for final sizing decisions; conclusions drawn from
estimates say so. Complement this report with: helm lint && helm template | kubeconform, a policy
engine (Polaris/Kyverno/OPA), and real load-test data with GC logs (-Xlog:gc* / -XX:+PrintGCDetails)
and kubectl top / VPA recommendations.

====================================================================================================
                                           END OF REPORT                                            
====================================================================================================
